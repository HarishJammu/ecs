import { Montserrat, Cormorant, Syne, Mulish } from 'next/font/google';

export const montserrat = Montserrat({
    subsets: ['latin'],
    weights: ['400', '700'], // specify weights you need
    display: 'swap',
});

export const cormorant = Cormorant({
    subsets: ['latin'],
    weights: ['400', '700'],
    display: 'swap',
});

export const syne = Syne({
    subsets: ['latin'],
    weights: ['400', '700'],
    display: 'swap',
});

export const mulish = Mulish({
    subsets: ['latin'],
    weights: ['400', '700'],
    display: 'swap',
});